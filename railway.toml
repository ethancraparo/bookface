[build]
buildCommand = "npm run build"

[deploy]
startCommand = "npx prisma migrate resolve --applied 0_init 2>/dev/null; npx prisma migrate deploy && npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
